/* eslint-disable arrow-body-style */
const service = require('./delete-feature.service');

describe('Service - deleteFeature', () => {
  beforeEach(() => {});

  afterEach(() => {});

  it('TODO: should do unit test for ', () => {
    service();
  });
});
