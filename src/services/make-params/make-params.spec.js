/* eslint-disable arrow-body-style */
const service = require('./make-params.service');

describe('Service - makeParams', () => {
  beforeEach(() => {});

  afterEach(() => {});

  it('TODO: should do unit test for ', () => {
    service({
      params: { z: 1, x: 2, y: 3 },
      query: {}
    });
  });
});
