/* eslint-disable arrow-body-style */
const service = require('./new-feature.service');

describe('Service - newFeature', () => {
  beforeEach(() => {});

  afterEach(() => {});

  it('TODO: should do unit test for ', () => {
    service();
  });
});
