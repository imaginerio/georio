/* eslint-disable arrow-body-style */
const service = require('./get-features.service');

describe('Service - getFeatures', () => {
  beforeEach(() => {});

  afterEach(() => {});

  it('TODO: should do unit test for ', () => {
    service();
  });
});
