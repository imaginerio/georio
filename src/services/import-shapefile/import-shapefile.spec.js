/* eslint-disable arrow-body-style */
const service = require('./import-shapefile.service');

describe('Service - importShapefile', () => {
  beforeEach(() => {});

  afterEach(() => {});

  it('TODO: should do unit test for ', () => {
    service();
  });
});
