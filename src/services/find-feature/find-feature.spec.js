/* eslint-disable arrow-body-style */
const service = require('./find-feature.service');

describe('Service - findFeature', () => {
  beforeEach(() => {});

  afterEach(() => {});

  it('TODO: should do unit test for ', () => {
    service();
  });
});
