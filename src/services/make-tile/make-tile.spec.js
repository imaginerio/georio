/* eslint-disable arrow-body-style */
const service = require('./make-tile.service');

describe('Service - makeTile', () => {
  beforeEach(() => {});

  afterEach(() => {});

  it('TODO: should do unit test for ', () => {
    service({ z: 14, x: 3852, y: 6771 }, 'Lines', ['id', 'firstyear', 'lastyear', 'name']);
  });
});
