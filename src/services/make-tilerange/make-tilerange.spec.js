/* eslint-disable arrow-body-style */
const service = require('./make-tilerange.service');

describe('Service - makeTilerange', () => {
  beforeEach(() => {});

  afterEach(() => {});

  it('TODO: should do unit test for ', () => {
    service();
  });
});
