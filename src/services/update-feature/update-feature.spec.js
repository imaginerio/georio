/* eslint-disable arrow-body-style */
const service = require('./update-feature.service');

describe('Service - updateFeature', () => {
  beforeEach(() => {});

  afterEach(() => {});

  it('TODO: should do unit test for ', () => {
    service();
  });
});
